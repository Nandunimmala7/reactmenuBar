import React from "react";
import './Button.css'
const STYLE = [
    'btn--primary',
    'btn--outline'
]
const SIZES = [
    'btn--menu',
    'btn--large'
]
export const button = ({
    children,
    type,
    onClick,
    buttonStyle,
    buttonSize
})=>{
    const 
}